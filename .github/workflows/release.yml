name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.artifact }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-14
            artifact: macos-arm64
            archive: tar.gz
          - runner: macos-13
            artifact: macos-x64
            archive: tar.gz
          - runner: windows-latest
            artifact: windows-x64
            archive: zip

    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.12.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm test
      - run: pnpm build
      - name: Prepare clean runtime dependencies
        shell: bash
        run: |
          rm -rf release-runtime
          mkdir release-runtime
          cp package.json pnpm-lock.yaml release-runtime/
          pnpm --dir release-runtime install --prod --frozen-lockfile --ignore-workspace --ignore-scripts --config.node-linker=hoisted
          cp -R build dist LICENSE README.md release-runtime/
      - run: node scripts/assemble-release.mjs release

      - name: Create tarball
        if: matrix.archive == 'tar.gz'
        shell: bash
        run: tar -czf "skill-manager-${{ github.ref_name }}-${{ matrix.artifact }}.tar.gz" -C release .
      - name: Create zip
        if: matrix.archive == 'zip'
        shell: pwsh
        run: Compress-Archive -Path release\* -DestinationPath "skill-manager-${{ github.ref_name }}-${{ matrix.artifact }}.zip"
      - uses: actions/upload-artifact@v4
        with:
          name: skill-manager-${{ matrix.artifact }}
          path: skill-manager-${{ github.ref_name }}-${{ matrix.artifact }}.${{ matrix.archive }}

  publish:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: release-assets/*
          generate_release_notes: true
